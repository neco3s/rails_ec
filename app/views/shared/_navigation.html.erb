<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container px-4 px-lg-5">
    <a class="navbar-brand" href="#!">無印良品みたいなサイト</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if request.path_info == '/products' || request.path_info == '/' %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
          <li class="nav-item"><%= link_to("ホームページ",root_path, class: 'text-decoration-none text-dark nav-link') %></li>
        </ul>
      <% elsif request.path_info == '/admin/products' %>
         <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
          <li class="nav-item"><%= link_to("商品一覧",admin_products_path, class: 'text-decoration-none text-dark nav-link') %></li>
          <li class="nav-item"><%= link_to("商品登録",new_admin_product_path, class: 'text-decoration-none text-dark nav-link') %></li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
<% flash.each do |key, value| %>
  <div class="flash <%= key %> animate-pulse">
    <%= value %>
  </div>
<% end %>
